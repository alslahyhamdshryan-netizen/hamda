# عبد الإله HR — نظام إدارة الموارد البشرية

نظام عربي لإدارة الموظفين والحضور والإجازات والرواتب والمهام والتقارير، بواجهة React مرتبطة بقاعدة بيانات Django.

## النماذج

- `Employee`: ملف الموظف والرقم الوظيفي والقسم والمسمى والراتب والحالة.
- `Department`: الأقسام ومديروها.
- `Attendance`: الحضور والانصراف والحالة اليومية.
- `LeaveRequest`: طلبات الإجازات والاعتماد والرفض.
- `Payroll`: الرواتب والبدلات والاستقطاعات وصافي الراتب.
- `Task`: مهام الموظفين ومتابعة الإنجاز.
- `UserProfile`: أدوار مدير النظام، مسؤول الموارد البشرية، والموظف.

## التشغيل

```bash
python3 -m pip install -r requirements.txt
python manage.py migrate
python manage.py createsuperuser
python manage.py runserver 8000
```

في طرفية أخرى:

```bash
pnpm install
pnpm dev
```

تعمل لوحة Django على `http://127.0.0.1:8000/admin/`، وتعمل واجهة النظام غالبًا على `http://localhost:5173` أثناء التطوير.

## الصلاحيات

يتم تسجيل الدخول من خلال Django Sessions، ويُرسل CSRF token مع طلبات POST. المدير ومسؤول الموارد البشرية يستطيعان إدارة ملفات الموظفين واعتماد الإجازات، بينما الموظف يطّلع على ما تسمح به صلاحياته. يجب ضبط `is_staff` و`is_superuser` بعناية من Django Admin.

## الإنتاج

غيّر `SECRET_KEY`، عطّل `DEBUG`، اضبط `ALLOWED_HOSTS`، استخدم PostgreSQL أو MySQL، فعّل HTTPS، وأضف سجل تدقيق ونسخًا احتياطية قبل تشغيل النظام على بيانات فعلية.
